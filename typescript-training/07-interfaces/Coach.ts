// an interface is a contract that defines the properties and methods that an object must implement
// Interfaces are used to define the shape of an object
export interface Coach {

    getDailyWorkout(): string;
    
}